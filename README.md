# Telegram-бот модерации (MVP)

Простой MVP-бот для модерации контента в AI Telegram-канале.

## Возможности

- `/start` доступен только администратору
- `/draft` создаёт тестовый черновик и отправляет его на модерацию
- `/generate [source_url]` создаёт черновик для Telegram через настроенный AI-провайдер (предпочтительно OpenRouter) на русском для `@simplify_ai`
- Если администратор отправляет боту обычное сообщение с URL, бот автоматически:
  - извлекает заголовок и читаемый текст страницы,
  - делает summary и генерирует черновик в стиле `@simplify_ai`,
  - сохраняет черновик с `source_url`,
  - пытается сохранить картинку превью источника из `og:image`, `twitter:image` или `twitter:image:src`,
  - возвращает его админу с кнопками модерации.
- Добавлена базовая защита от дублей по `source_url` (повторная ссылка не создаёт новый черновик)
- Сгенерированные черновики используют правила стиля из `prompts/post_style.md` (человечный тон, краткость, без канцелярита)
- Кнопки модерации в интерфейсе:
  - ✅ Опубликовать
  - 🗓️ Запланировать
  - 📅 В ближайший слот
  - 🔗 Открыть источник (если у черновика есть `source_url`)
  - ♻️ Перегенерировать (если черновик создан из `source_url`)
  - 🖼 Прикрепить картинку источника (если такая картинка сохранена и медиа ещё не прикреплено)
  - ❌ Отклонить
  - ✏️ Редактировать текст
  - ✨ Улучшить Claude
  - 🧹 Убрать воду
  - 📉 Сделать короче
  - 😐 Без рекламного тона
- Фокусные rewrite-кнопки чистят текст Telegram-черновика на месте: убирают воду, сокращают или нейтрализуют рекламный тон без создания нового черновика. Медиа, `source_url`, `source_image_url` и данные расписания сохраняются.
- Для Schedule доступны слоты: `10:00`, `14:00`, `18:00`, `21:00`
- Публикация одобренного контента в канал
- Черновики поддерживают опциональные `media_url` и `media_type` (`photo`, `animation`, `video`), а также отдельный `source_image_url` для картинки превью источника
- Превью модерации показывает медиа, если оно прикреплено к черновику
- Публикация в канал отправляет media+caption, а без медиа отправляет текст
- Если текст поста с медиа длиннее лимита caption Telegram, бот публикует медиа с короткой подписью и отправляет полный текст отдельным сообщением
- Фоновый scheduler каждые 60 секунд проверяет запланированные черновики и публикует их в `CHANNEL_ID`
- Сохранение текста черновика, статуса, `source_url`, `scheduled_at`, `media_url` и `media_type` в SQLite
- Статусы черновиков: `draft`, `approved`, `scheduled`, `published`, `rejected`
- Автосбор тем по AI через `/collect` из RSS, публичных страниц и опциональных API-источников (OpenAI, Anthropic, Google AI, Perplexity, Hugging Face, GitHub Trending AI)
- Reddit community RSS отключены по умолчанию: Reddit часто блокирует серверное чтение страниц, из-за чего админский сценарий «тема → черновик» становится нестабильным. Старые сохранённые Reddit-темы не удаляются автоматически и могут оставаться видимыми до отклонения или отдельной очистки.
- X можно подключить только через официальный X API: включи `ENABLE_X_SOURCES`, задай `X_API_BEARER_TOKEN`, вручную перечисли аккаунты в `X_ACCOUNTS` без `@` и при необходимости измени `X_MAX_POSTS_PER_ACCOUNT`. Бот не скрейпит HTML `x.com`, не ходит в X без явного включения и нормально работает без X-настроек. X API может тарифицироваться по usage-based модели на стороне X.
- `MAX_TOPIC_AGE_DAYS` управляет свежестью тем: старые RSS-материалы пропускаются, отсутствующие даты обрабатываются безопасно, а скоринг редакционно оптимизирован под `@simplify_ai` (полезные AI-инструменты, практичные новости, мемы и простые объяснения для новичков).
- Topic candidates хранят русские поля для карточек (`title_ru`, `summary_ru`, `angle_ru`, `reason_ru`), чтобы админ сразу видел, о чем тема, почему она интересна и какой пост можно сделать
- Для GitHub Trending карточки сохраняют имя репозитория как есть, но детерминированно очеловечивают распространённые английские описания в короткий русский заголовок и понятное поле `О чем` с языком и stars.
- Оригинальный заголовок источника, URL и описание источника сохраняются отдельно: карточка показывает `Оригинал:` только когда русский display-заголовок отличается
- Если enrichment/перевод недоступен или не сработал, бот без ошибки показывает исходный заголовок, URL и безопасное общее русское объяснение по типу источника
- `/collect` показывает русские компактные превью тем; после сбора появляются кнопки `🧠 Открыть темы`, `🔥 Горячие` и `🆕 Лучшие новые`, чтобы сразу перейти к навигации по темам.
- AI-обогащение тем во время `/collect` ограничено `TOPIC_AI_ENRICH_LIMIT` и `TOPIC_AI_TRANSLATE_LIMIT` (по умолчанию 8): меньшие значения ускоряют сбор, `0` отключает AI-обогащение.
- `/topics` показывает последние topic candidates с кнопками `✍️ Создать черновик`, `🔗 Открыть источник` и `❌ Отклонить тему`
- `/attach_media <draft_id> <photo|video|animation> <media_url>` — прикрепить медиа к черновику вручную
- `/drafts [status]` — показать последние черновики, опционально с фильтром по статусу
- `/draft_info <id>` — открыть полную информацию по черновику
- `/delete_draft <id>` — удалить черновик
- `/cleanup_preview` — показать предварительный расчёт безопасной очистки старых тем и черновиков
- `/cleanup_confirm` — применить последний предпросмотр очистки, если он создан не более 10 минут назад
- Если администратор отвечает на сообщение модерации фото/анимацией/видео, бот сохраняет Telegram `file_id` в черновик
- Дедупликация тем по URL и похожему story key: бот группирует повторяющиеся topic candidates из разных источников, хранит связанные источники/URL в одной истории и поэтому уменьшает дубли в `/topics_hot` и дневных планах.
- Работа через long polling (подходит для Railway worker service)

## Быстрые действия в Telegram

- В карточках тем есть кнопка `🔗 Открыть источник`, чтобы быстро проверить первоисточник перед созданием черновика.
- Черновик можно поставить в ближайший свободный слот кнопкой `📅 В ближайший слот`: бот учитывает `DAILY_POST_SLOTS`, `SCHEDULE_TIMEZONE`, будущие слоты сегодня и уже занятые слоты в очереди.
- Черновики, созданные из `source_url`, можно перегенерировать на месте кнопкой `♻️ Перегенерировать`; существующие медиа при этом сохраняются.
- Когда черновик создаётся из URL, бот пытается сохранить картинку превью источника. Она не прикрепляется автоматически: администратор может вручную нажать `🖼 Прикрепить картинку источника`, если у черновика ещё нет медиа. Уже прикреплённое медиа никогда не перезаписывается автоматически.
- Workflow для Shorts/Reels/TikTok/видео-сценариев не добавлен: бот остаётся инструментом подготовки постов для Telegram-канала.


## Архитектура

- `main.py` — entrypoint приложения, регистрация команд и запуск scheduler.
- `bot/config.py` — загрузка и валидация env-настроек.
- `bot/database.py` — SQLite persistence для черновиков, тем и планирования.
- `bot/handlers.py` — Telegram-команды, callbacks и модерационные действия.
- `bot/writer.py` — генерация черновиков через AI и парсинг URL-источников.
- `bot/sources.py` и `bot/topic_scoring.py` — сбор и ранжирование тем.
- `bot/publisher.py` — публикация постов в Telegram-канал.
- `bot/telegram_formatting.py` — безопасное Telegram HTML-форматирование, ссылки, blockquote, custom emoji.
- `bot/media_utils.py` — вспомогательные функции для media group.
- `bot/style_guide.py` и `prompts/post_style.md` — правила стиля `@simplify_ai`.

## Требования

- Python 3.11+
- Telegram bot token от BotFather
- OpenRouter API key (рекомендуется для генерации и полировки)
- OpenAI API key (опциональный fallback)

## Установка

1. Клонируй репозиторий.
2. Создай и активируй виртуальное окружение.
3. Установи зависимости:

```bash
pip install -r requirements.txt
```

4. Создай env-файл:

```bash
cp .env.example .env
```

5. Заполни значения в `.env`:

- `BOT_TOKEN` — токен бота
- `ADMIN_ID` — числовой user ID администратора в Telegram
- `CHANNEL_ID` — username канала (пример: `@my_channel`) или id канала
- `OPENROUTER_API_KEY` — основной ключ для OpenRouter (предпочтительный провайдер)
- `OPENAI_API_KEY` — fallback-ключ OpenAI, если OpenRouter не задан
- `MODEL_DRAFT` — модель для обычных черновиков (по умолчанию `moonshotai/kimi-k2.6`)
- `MODEL_POLISH` — модель для улучшения черновиков (по умолчанию `anthropic/claude-sonnet-4.5`)
- `OPENROUTER_SITE_URL` — опциональный Referer для OpenRouter
- `OPENROUTER_APP_NAME` — имя приложения для OpenRouter (по умолчанию `Simplify AI Autopilot`)
- `SCHEDULE_TIMEZONE` — таймзона для планирования (по умолчанию `Europe/Moscow`)
- `DB_PATH` — путь к SQLite-файлу черновиков (по умолчанию `data/drafts.db`)
- `CHANNEL_ID` должен быть только в формате `@channel_username` или числового id (`-100...` для приватных каналов), invite-ссылки (`https://t.me/+...`, `t.me/joinchat/...`) не подходят.
- `POST_MAX_CHARS` — жёсткий максимум длины поста (по умолчанию `1400`, минимум `500`)
- `POST_SOFT_CHARS` — желательная длина поста (по умолчанию `1100`, минимум `400`, не больше `POST_MAX_CHARS`)
- `MAX_TOPIC_AGE_DAYS` — фильтр свежести тем (по умолчанию `14`, допустимо `1..60`): старые RSS items не попадают в базу, темы без даты не ломают сбор и получают мягкую редакционную проверку в скоринге.
- `ENABLE_REDDIT_SOURCES` — включает старые Reddit RSS-источники, если явно поставить `1`, `true`, `yes` или `on`; по умолчанию `false`, потому что Reddit часто блокирует серверное чтение.
- `ENABLE_X_SOURCES` — включает опциональный сбор из X только через официальный API; по умолчанию `false`.
- `X_API_BEARER_TOKEN` — bearer token для X API. Если пустой, X-источник пропускается без падения бота.
- `X_ACCOUNTS` — вручную заданный comma-separated список usernames без `@`; аккаунты в коде не захардкожены.
- `X_MAX_POSTS_PER_ACCOUNT` — сколько последних постов брать на аккаунт (по умолчанию `5`, допустимо `1..20`); бот не пагинирует, не читает треды рекурсивно и не забирает медиа отдельно.
- `OPENAI_API_KEY` можно не задавать: бот запускается и работает без него (`/start`, `/draft`, модерация, отклонение, переписывание, публикация).
- Команда `/generate` требует `OPENROUTER_API_KEY` или `OPENAI_API_KEY`; OpenRouter используется в приоритете.
- `OPENROUTER_API_KEY` храни только в переменных Railway, не в репозитории.

## Команды

- `/start` — приветствие бота (только для администратора)
- `/draft` — создание тестового черновика (для проверки)
- `/generate` — создание AI-черновика через `MODEL_DRAFT` (например, Kimi)
- `/generate https://example.com/article` — создание AI-черновика и сохранение source URL в БД
- `/collect` — собрать свежие темы-кандидаты
- `/topics` — показать последние темы с кнопками `✍️ Создать пост`
- `/attach_media <draft_id> <photo|video|animation> <media_url>` — прикрепить медиа к черновику
- `/drafts` — показать последние 10 черновиков
- `/drafts <status>` — показать черновики по статусу (`draft`, `approved`, `scheduled`, `published`, `rejected`)
- `/draft_info <id>` — показать полный черновик и кнопки модерации
- `/delete_draft <id>` — удалить черновик
- `/cleanup_preview` — показать предварительный расчёт безопасной очистки старых тем и черновиков
- `/cleanup_confirm` — применить последний предпросмотр очистки, если он создан не более 10 минут назад
- `https://example.com/article` (обычным сообщением) — авто-создание черновика из содержимого страницы

`source_url` показывается в сообщениях модерации, чтобы администратор мог проверить контекст. Для собранных тем админский интерфейс предпочитает русский display-заголовок, но исходный title и URL источника остаются в базе и показываются в карточке темы, если русский заголовок отличается. В финальный текст поста строка `Источник:` автоматически не добавляется и при необходимости удаляется перед сохранением.

В модерации черновиков доступны быстрые cleanup-действия `🧹 Убрать воду`, `📉 Сделать короче` и `😐 Без рекламного тона`. Они обновляют тот же черновик на месте, сохраняют прикреплённые медиа и ссылки источника, а после переписывания возвращают обновлённое превью с теми же кнопками.

Очередь `/queue_today` и `/queue_tomorrow` показывает все настроенные дневные слоты: свободные и занятые, с коротким превью запланированного поста. Из свободного слота администратор может вручную выбрать существующий черновик в статусе `draft` или `approved` и поставить его на конкретное время. Такая постановка только добавляет черновик в расписание и не публикует пост немедленно.

Если обычную ссылку нельзя получить (битая ссылка, блокировка, не-HTML контент и т.д.), бот отправит понятную ошибку и не создаст черновик. Для сохранённых кандидатов тем есть отдельный fallback: если источник блокирует чтение страницы, особенно старые Reddit-темы, бот может создать черновик по уже сохранённым метаданным темы. Если и такой fallback вернул пустой результат для Reddit, бот показывает отдельное русское объяснение, не создаёт черновик и не помечает тему использованной. Такие fallback-черновики требуют фактчекинга перед публикацией, потому что полный источник не был прочитан напрямую.


## Безопасная очистка базы

Команда `/cleanup_preview` ничего не удаляет: она показывает короткий русский отчёт по категориям — старые отклонённые, использованные и новые темы, старые `rejected`/`failed` черновики и залежавшиеся нетронутые `draft`-черновики. После предпросмотра бот сохраняет краткое состояние подтверждения и показывает кнопки `🧹 Очистить` и `Отмена`.

Команда `/cleanup_confirm` или кнопка `🧹 Очистить` применяет только свежий предпросмотр; если прошло больше 10 минут, нужно снова выполнить `/cleanup_preview`. Очистка консервативная: она не запускается автоматически при старте, не меняет очередь публикаций и не трогает актуальные свежие записи.

Защищены от удаления черновики в статусах `scheduled`, `publishing`, `published` и `approved`, любые черновики с прикреплёнными `media_url`/`media_type`, а также черновики с отсутствующей или некорректной датой обновления. Данные `ai_usage` не удаляются. Метаданные источников и картинок у активных черновиков сохраняются, потому что активные черновики не попадают в очистку.

## Восстановление зависших публикаций

Если Railway перезапустит процесс во время отложенной публикации, черновик может остаться в промежуточном статусе `publishing`. При следующем старте бот один раз переводит такие черновики в `failed`, очищает время расписания и пишет количество восстановленных записей в лог.

Бот не публикует эти черновики автоматически повторно: сообщение могло уже уйти в Telegram, а процесс мог упасть до отметки `published` в базе. Ручная проверка через `failed` безопаснее и помогает избежать дублей в канале.

Администратор может открыть список командой `/failed_drafts`, проверить каждый черновик кнопкой `Открыть`, а затем вернуть нужный текст в обычные черновики кнопкой восстановления или командой `/restore_draft ID`.

## Локальный запуск

```bash
python main.py
```

## Примечания по деплою в Railway

Используй такую стартовую команду:

```bash
python main.py
```

Важно:
- Разворачивай бота как **worker/background service** (долгоживущий процесс).
- Для приватных каналов используй числовой `CHANNEL_ID` вида `-100...` (а не invite-ссылку).
- Если нужно сохранять черновики и отложенные публикации после рестарта, размести SQLite-файл на persistent storage / volume и укажи путь через `DB_PATH` (например, `/data/drafts.db`).
- SQLite в боте использует WAL и `busy_timeout` для более надёжной работы при частых обращениях к базе, но WAL не заменяет persistent storage.
- На Railway путь `data/drafts.db`/`./data/drafts.db` без volume не персистентный: база может потеряться после redeploy.
- Отложенные публикации проверяются каждые 60 секунд.
- Задай переменные окружения в настройках проекта Railway:
  - `BOT_TOKEN`
  - `ADMIN_ID`
  - `CHANNEL_ID`
  - `OPENROUTER_API_KEY` (рекомендуется)
  - `OPENAI_API_KEY` (опциональный fallback)
  - `MODEL_DRAFT`
  - `MODEL_POLISH`
  - `OPENROUTER_SITE_URL` (опционально)
  - `OPENROUTER_APP_NAME` (опционально)
  - `DB_PATH` (опционально; по умолчанию `data/drafts.db`)

## Безопасность

- Никогда не коммить `.env` с реальными токенами и секретами.
- В `.env.example` должны быть только плейсхолдеры.


## Форматирование цитат
- Для перечислений можно писать обычные подряд идущие строки с `➖`.
- При публикации бот автоматически превращает 2+ последовательных пункта списка в Telegram `<blockquote>`.
- Внутренние маркеры `[[QUOTE]] ... [[/QUOTE]]` по-прежнему поддерживаются.
- Custom emoji aliases `[[EMOJI:alias]]` безопасно превращаются в Telegram custom emoji, если настроены ID; иначе известные aliases заменяются обычными emoji, а неизвестные маркеры удаляются.
- Старые псевдо-маркеры `▌` автоматически очищаются перед модерацией и публикацией.
- Любой raw HTML из ответа модели экранируется и не проходит в публикацию.
